// INSTALLATION
$ sudo apt-get update
$ sudo apt-get -y install build-essential curl
$ sudo apt-get -y install libpcre3-dev zlib1g-dev libssl1.0-dev libnghttp2-dev libapr1-dev libaprutil1-dev
$ sudo ./configure --prefix=/usr/local/httpd2  --enable-so --enable-ssl --enable-http2
$ sudo make -j
$ sudo make install

// SSL ISSUANCE (https://codens.info/1805)
1. generate localhost.conf
	change name in commonName_default and DNS.1 to localhost
2. generate localhost.key and localhost.crt
	$ mv ~/serverset
	$ sudo openssl req -config localhost.conf -new -sha256 -newkey rsa:2048 -nodes -keyout localhost.key -x509 -days 365 -out localhost.crt
	$ enters...

// SERVER SETTING
** /usr/local/httpd2/conf/httpd.conf **
1. find & erase 4 comments below
[line 89] LoadModule socache_shmcb_module modules/mod_socache_shmcb.so
[line 135] LoadModule ssl_module modules/mod_ssl.so 
[line 136] LoadModule http2_module modules/mod_http2.so
[line 497] Include conf/extra/httpd-ssl.conf
1-1. change configure
[line 221] DocumentRoot "/usr/share/nginx/html"
[line 222] <Directory "/usr/share/nginx/html">
2. add at the last
<IfModule http2_module> 
  ProtocolsHonorOrder On
  Protocols h2 h2c http/1.1
</IfModule>
** /usr/local/httpd/conf/extra/httpd-ssl.conf **
3. change configure
[line 93] SSL session timeout to 10s?
[line 124] DocumentRoot "/usr/share/nginx/html"
[line 125] ServerName localhost:443
4. move .crt and .key file to conf directory
	$ sudo cp localhost.crt /usr/local/httpd2/conf/server.crt
	$ sudo cp localhost.key /usr/local/httpd2/conf/server.key

// START & STOP
$ sudo cp ./httpd.conf /usr/local/httpd/conf
$ sudo cp ./httpd-ssl.conf /usr/local/httpd/conf/extra
$ sudo /usr/local/httpd2/bin/httpd
$ sudo curl -k -I https://localhost
$ sudo /usr/local/httpd2/bin/httpd -k stop



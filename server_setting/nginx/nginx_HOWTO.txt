// INSTALLATION
$ sudo apt-get update
$ sudo apt-get -y install build-essential curl libpcre3-dev zlib1g-dev libssl1.0-dev
$ sudo ./configure --sbin-path=/usr/local/nginx/nginx --conf-path=/usr/local/nginx/nginx.conf --pid-path=/usr/local/nginx/nginx.pid --with-http_ssl_module --with-stream --with-mail=dynamic --with-cc-opt="-g" --with-debug --with-http_v2_module
$ sudo make
$ sudo make install

// SSL ISSUANCE (https://codens.info/1805)
1. generate localhost.conf
	change name in commonName_default and DNS.1 to localhost
2. generate localhost.key and localhost.crt
	$ mv ~/serverset
	$ sudo openssl req -config localhost.conf -new -sha256 -newkey rsa:2048 -nodes -keyout localhost.key -x509 -days 365 -out localhost.crt
	$ enters...

// SERVER SETTING
1. change nginx.conf (/usr/local/nginx/nginx.conf)
[line 99] 443 ssl http2
[line 100] localhost
[line 102] /home/oren/PRETT2/server_keys/localhost.crt;
[line 103] /home/oren/PRETT2/server_keys/localhost.key;
[lines 98-115] de-omit lines 
2. change nginx.conf (/usr/local/nginx/nginx.conf)
- for nginx >1.19.7
[line 31] keepalive_timeout  10s; (changing 65 -> 10)
- for Nginx <1.19.7 
[line 31] http2_idle_timeout 10s; (disable keepalive_timeout)
[line 107] http2_recv_timeout 10s;

// START & STOP
$ sudo /usr/local/nginx/nginx
$ sudo curl -k -I https://localhost
$ sudo /usr/local/nginx/nginx -s stop

// SPECIAL CASE: installing NGINX 1.14.0 in Ubuntu 22
- install gcc 7 with update-alternatives
- erase salt something
- use openssl 1.1.1j
$ sudo wget https://www.openssl.org/source/openssl-1.1.1l.tar.gz
- specify --with-openssl to openssl source directory 
$ sudo ./configure --sbin-path=/usr/local/nginx/nginx --conf-path=/usr/local/nginx/nginx.conf --pid-path=/usr/local/nginx/nginx.pid --with-http_ssl_module --with-stream --with-mail=dynamic --with-cc-opt="-g" --with-debug --with-http_v2_module --with-openssl=/home/oren/PRETT2/server_setting/nginx/openssl-1.1.1l
